<% from_user_shift = UserShift.find(notification.data['from_user_shift_id']) %>
<% to_user_shift = UserShift.find(notification.data['to_user_shift_id']) %>

<%= from_user_shift.user.email %> wants to trade shifts with you. Are you able trade from:
</br>
<%= display_shift_time(from_user_shift.shift.start_at, from_user_shift.shift.end_at) %>
<b>to</b>
<%= display_shift_time(to_user_shift.shift.start_at, to_user_shift.shift.end_at) %>
?
<%= button_to 'Yes', user_shifts_update_selections_path, method: :put, params: { 'shift_ids[]' => [1] } %>
<%= button_to 'No', user_shifts_update_selections_path, method: :put, params: { 'shift_ids[]' => [1] } %>
